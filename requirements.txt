# Core
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0
python-dateutil>=2.9.0
pytz>=2025.2
tzdata>=2025.3

# Viz
matplotlib>=3.8.0
seaborn==0.13.2
plotly==6.4.0
kaleido==0.2.1

# ML
scikit-learn>=1.4.0
joblib>=1.3.0
xgboost==2.1.3

# Deep Learning
torch>=2.2.0

# Synthetic data
sdv==1.32.1
ctgan==0.11.1
copulas==0.14.0
rdt==1.19.0
deepecho==0.8.0

# Metrics / helpers
dython>=0.7.12

# HPO
optuna>=4.0.0

# Notebook runtime
ipykernel>=6.29.0
notebook>=7.4.0
jupyter>=1.1.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0.0
psutil>=5.9.0
packaging>=24.0
cloudpickle>=3.1.0
networkx>=3.3
graphviz>=0.21

# Synthetic data models — GANerAid + tab-gan-metrics
# These MUST be installed with --no-deps to avoid dython version conflicts.
# setup_env.sh handles this automatically. If installing manually:
#   pip install --no-deps GANerAid==1.8
#   pip install --no-deps tab-gan-metrics
# Then apply patches from patches/ to fix the GANerAid device bug
# and the tab-gan-metrics dython import incompatibility.
GANerAid==1.8
Faker>=40.0.0
